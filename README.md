# CLI Calculator
A simple, powerful, scriptable, CLI calculator.

## Installation
```
cd $HOME
git clone https://github.com/yamunv11/cli-calc
cd cli-calc
make install
```

## TODO
- refactor to an abstract syntax tree (AST)
- add support for user defined functions
