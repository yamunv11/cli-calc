# CLI Calculator
A simple, powerful, scriptable, CLI calculator.

## Installation
```
cd $HOME
git clone https://github.com/yous3fghazyv11/cli-calc
cd cli-calc
sudo make install
```

## TODO
- add support for variables
- add support for numbers with `,` inside
- clean up the code
